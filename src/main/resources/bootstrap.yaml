spring:
  config:
    import: vault://secret/prod/auth-service
  cloud:
    vault:
      uri: http://vault:8200
      authentication: token
      token: root
      kv:
        enabled: true
        backend: secret
        application-name: prod/auth-service
      enabled: true